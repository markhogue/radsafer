## Revision purpose
This revision adds an argument to the RN_plot_spectrum function.

## Test environments
* local Windows 11 home version 25H2
* passed all checks with devtools::check_win_devel()

## R CMD check results
There were no ERRORs, WARNINGS or NOTEs when running devtools::check() with default parameters. 
